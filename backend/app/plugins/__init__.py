"""User/community plugins.

Any package in this directory exposing `Tool` subclasses is auto-registered
at startup by app.tools.registry — same base class as built-in tools, no
separate plugin API. See README.md here and docs/ARCHITECTURE.md section 6.
"""
