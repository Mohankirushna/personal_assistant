"""Git tool: status, diff, add, commit, branch, log. Destructive operations (force-push, hard
reset) are risk_level=DESTRUCTIVE.

Phase 1 stub. Implemented in Phase 6 (Tools). Implements the `Tool` interface
from app.tools.base.
"""

from __future__ import annotations
